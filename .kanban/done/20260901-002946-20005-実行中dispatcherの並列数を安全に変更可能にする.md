---
id: 20260901-002946-20005
title: 実行中dispatcherの並列数を安全に変更可能にする
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
resolve_max_attempts: 2
attempts: 0
resolve_attempts: 0
created: 2026-09-01T00:29:46
---

## Task

対象: kanban.sh、kanban-secretary.sh、README.md、関連テスト\n\n現在 kanban run は jobs_max を起動時に一度だけ読み、実行中に .kanban/KANBAN.md の jobs を変えても反映されない。visible Herdr dispatcherを停止・重複実行せず、実行中に並列上限を安全に変更できる仕組みを実装する。\n\n要件:\n- secretary dispatch で起動した通常runは、project policyの jobs 変更を実行中に検知し、新しい上限を次のdispatch loopから適用する\n- 2→6の増加時は、既存workerを中断せず空き枠へtodoカードを追加起動する\n- 減少時は実行中workerをkillせず、新規起動だけを抑える\n- 明示的な -j/--jobs の優先順位を定義し、READMEに記載する\n- dispatcher二重起動は禁止のまま\n- Bash 3.2互換を維持する\n- focused testsを追加し、fast tierを1回、必要な関連テストを実行する。full suiteの反復実行は不要

## History

### 2026-09-01 01:29:00 direct integration

ユーザー指示でdispatcherを停止後、mainへ直接実装・検証済み。KANBAN.md jobsのlive再読込、増加時の追加起動、減少時の既存job維持、-j/env固定をE2E確認。full gate合格。
