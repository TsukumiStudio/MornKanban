---
id: 20260831-181430-6200
title: setup_gui (tkinter): アンインストールボタン追加
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T18:14:30
---

## Task

## 目的
gui/setup_gui.py の1ウィンドウに [アンインストール] ボタンを追加する。他ファイルは触らない。

## 契約 (setup_core.py に実装済み)
run_uninstall() -> list[str]: CLI symlink とスキルを安全に削除 (インストーラ管理物のみ)、結果メッセージのリスト

## 変更内容
- [セットアップ実行] の横に [アンインストール] ボタン。run_uninstall() の結果を既存の結果表示ラベルに複数行表示し、状態表示を再描画
- ウィンドウは1つのまま。追加ダイアログを出さない (誤操作防止の確認は不要 — run_uninstall は安全側設計)

## 完了条件・検証
- python3 -m py_compile gui/setup_gui.py
- grep で run_uninstall の呼び出しが存在
- import 副作用なし (python3 -c "import sys; sys.path.insert(0,'gui'); import setup_gui" が通る)

## History

### 2026-08-31 18:15:04 worker output (tail)

  最終回答をファイルに書き込む。

⏺ Write(.kanban-answer.md)
  ⎿  Wrote 14 lines to .kanban-answer.md
       1 ## 実施内容
       3 gui/setup_gui.py に [アンインストール] ボタンを追加した。他ファイ
         ルは未変更。
       5 - `setup_core` の import に `run_uninstall` を追加
       6 - `on_run_uninstall()` を新設: `run_uninstall()` の結果メッセージ
          で状態表示 (deps_var / step1_var / step2_var) を再描画。OSError
         は messagebox.showerror で表示
         アンインストール] ボタンを追加 (fill=tk.X, expand=True で等幅)
       8 - 確認ダイアログなし。ウィンドウは1つのまま
      10 ## 検証結果
     … +11 lines

⏺ gui/setup_gui.py に [アンインストール] ボタンを追加完了。全検証通過(py_compile /
  grep / import副作用なし)。回答は .kanban-answer.md にも書き込み済み。

✻ Churned for 27s · done 6:15 PM

──────────────────────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────────────────────
  20260831-181430-6200  kanban/20260831-181430-6200  Sonnet 5                /rc
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents
## 実施内容

gui/setup_gui.py に [アンインストール] ボタンを追加した。他ファイルは未変更。

- `setup_core` の import に `run_uninstall` を追加
- `on_run_uninstall()` を新設: `run_uninstall()` の結果メッセージを既存の `result_var` (結果表示ラベル) に複数行表示し、`refresh()` で状態表示 (deps_var / step1_var / step2_var) を再描画。OSError は messagebox.showerror で表示
- [セットアップ実行] ボタンを `button_row` フレームで包み、横に [アンインストール] ボタンを追加 (fill=tk.X, expand=True で等幅)
- 確認ダイアログなし。ウィンドウは1つのまま

## 検証結果

- `python3 -m py_compile gui/setup_gui.py` → OK
- `grep -n "run_uninstall" gui/setup_gui.py` → import (17行目)、呼び出し (65行目) を含め4箇所ヒット
- `python3 -c "import sys; sys.path.insert(0,'gui'); import setup_gui"` → OK (import 副作用なし)

### 2026-08-31 18:15:40 review

score: 95 / threshold: 80

実装は契約通り。[アンインストール]ボタンをbutton_rowで横並び追加し、run_uninstall()の結果をresult_varに複数行表示、refresh()で再描画。確認ダイアログなし、ウィンドウ1つのまま。py_compile/grep/import副作用なしすべて通過を実測で再確認済み。他ファイルは未変更(pycはビルド成果物で対象外)。減点理由: setup_core.run_uninstallはin_worktree()時に['refused: kanban worktree 内']を返すが、GUI側はこの文字列をエラーメッセージボックスではなく通常の結果ラベルにそのまま表示するだけで、ユーザーには成功時と見分けがつきにくい(軽微、契約上は許容範囲)。
