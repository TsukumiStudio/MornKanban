---
id: 20260831-162123-26656
title: gui/server.py: セットアップ専用APIへ全面改稿
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T16:21:23
---

## Task

## 目的
gui/server.py を「かんばん管理 API」から「環境セットアップ専用 API」へ全面的に書き換える。
既存の board/card/policy/secretary/dispatch 系エンドポイントは**全て削除**する。

## 制約 (既存と同じ)
- python3 標準ライブラリのみ、単一ファイル、ThreadingHTTPServer、127.0.0.1 bind、
  ポートは MORNKANBAN_GUI_PORT (既定 8765)、静的配信は gui/static/、REPO は server.py の2階層上
- subprocess.run(timeout=30)。失敗は {"ok":false,"error":str} + 400/500

## API 契約 (新。フロントは別カードがこれだけを頼りに実装)
- GET /api/status → {"deps":{"herdr":bool,"claude":bool,"codex":bool,"python3":true},
  "install":{"cli":bool,"skill":bool},"repo":"<REPO絶対パス>","herdr_env":bool}
  - deps は shutil.which。install.cli は which("kanban") または ~/.local/bin/kanban が存在。
    install.skill は ~/.claude/skills/kanban-dispatch/SKILL.md が存在
- POST /api/install/cli → os.makedirs(~/.local/bin, exist_ok=True) し、~/.local/bin/kanban を
  REPO/kanban.sh へのシンボリックリンクに置き換え (既存リンクは付け替え、通常ファイルなら 400)。
  → {"ok":true,"in_path": "~/.local/bin" が PATH に含まれるか}
- POST /api/install/skill {"force":bool 省略可} → ~/.claude/skills/kanban-dispatch/SKILL.md を生成。
  既存かつ force でなければ {"ok":false,"error":"already installed (force で上書き)"} と 409。
  内容は次のとおり (REPO は絶対パスで埋め込む):
  ---
  name: kanban-dispatch
  description: "File-based kanban dispatch: card every implementation request and run the background dispatcher. Use when assigned implementation work in a project with .kanban/, or when asked to set up or operate kanban dispatch."
  user_invocable: true
  ---
  # kanban-dispatch
  The kanban CLI and the full workflow contract live in <REPO>.
  **Read <REPO>/README.md and follow it** (Secretary Bootstrap, Dialogue-Agent Contract, Model Policy, Herdr Integration).
  → {"ok":true,"path":"<書いた絶対パス>"}
- 設定 ~/.config/mornkanban/gui.json = {"projects":[...]} (既存仕様のまま)
- GET /api/projects → {"projects":[{"path","name","has_kanban"}]} (実在しないものは除外)
- POST /api/projects {"path"} → 実在ディレクトリのみ追加 → {"ok":true,"project":{...}}
- POST /api/init {"path"} → ["bash", REPO+"/kanban.sh", "init", path] → {"ok":true}
- 上記以外の /api/* は 404

## 完了条件・検証
- python3 -m py_compile gui/server.py が通る
- MORNKANBAN_GUI_PORT=8801 で起動し、curl で /api/status が deps と install を含むこと、
  /api/board が 404 になることを確認して kill

## History

### 2026-08-31 16:28:07 worker output (tail)


⏺ 結果

  gui/server.py を環境セットアップ専用 API へ全面書き換え。

  - board/card/policy/secretary/dispatch 系を全削除
  - 新規: GET /api/status (deps+install+repo+herdr_env)、POST /api/install/cli、POST
    /api/install/skill
  - 維持: GET/POST /api/projects、POST /api/init
  - それ以外の /api/* は404

  検証: py_compile 通過。ポート8801で起動し /api/status が deps/install を含むこと、/api/board
  が404になることを確認済み。サーバは kill 済みで残留なし。

  回答は .kanban-answer.md にも書き込み済み。

✻ Cooked for 2m 57s · done 4:28 PM

──────────────────────────────────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────────────────────────────────
  20260831-162123-26656  kanban/20260831-162123-26656  Sonnet 5                          /rc
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents
gui/server.py を「かんばん管理 API」から「環境セットアップ専用 API」へ全面書き換え。

## 変更内容
- board/card/policy/secretary/dispatch 系エンドポイントとヘルパー (herdr_env split_pane pane_id_from board_for read_frontmatter policy_path 等) を全削除
- 新API実装:
  - GET /api/status → deps(herdr/claude/codex/python3), install(cli/skill), repo, herdr_env
  - POST /api/install/cli → ~/.local/bin/kanban を REPO/kanban.sh へのシンボリックリンクに設置 (既存リンクは付け替え、通常ファイルなら400)
  - POST /api/install/skill {force} → ~/.claude/skills/kanban-dispatch/SKILL.md を生成 (既存かつforceでなければ409)
  - GET/POST /api/projects, POST /api/init は既存仕様のまま維持
  - 上記以外の /api/* は404 (PUT メソッドのルートも削除)

## 検証
- `python3 -m py_compile gui/server.py` 通過
- `MORNKANBAN_GUI_PORT=8801` で起動し curl 確認:
  - GET /api/status → `{"deps": {"herdr": true, "claude": true, "codex": true, "python3": true}, "install": {"cli": true, "skill": true}, "repo": "...", "herdr_env": true}`
  - GET /api/board → 404
- サーバプロセスは確認後 kill 済み (残留なし)

### 2026-08-31 16:46:49 review

score: 95 / threshold: 80

Implementation matches the API contract closely: GET /api/status, POST /api/install/cli (symlink replace, 400 on non-symlink), POST /api/install/skill (409 on existing without force, SKILL_TEMPLATE content matches spec verbatim), GET/POST /api/projects, POST /api/init all correct. All board/card/policy/secretary/dispatch code and helpers (read_frontmatter, board_for, pane_id_from, split_pane, api_policy_*, api_board, api_card_*, api_secretary, api_dispatch, do_PUT) were fully removed. Independently verified: py_compile passes, server started on 8802, GET /api/status returned correct deps/install/repo/herdr_env shape, GET /api/board returned 404, server was killed with no residual process. Minor nit: do_PUT was dropped entirely rather than returning JSON 404 for PUT/DELETE on /api/* (falls back to http.server's default 501 'Unsupported method' instead of the JSON error envelope), but the task's stated completion criteria (GET /api/status, GET /api/board 404) are satisfied and this isn't explicitly required by the contract.
