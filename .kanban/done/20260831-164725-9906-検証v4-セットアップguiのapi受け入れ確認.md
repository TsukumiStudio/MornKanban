---
id: 20260831-164725-9906
title: 検証v4: セットアップGUIのAPI受け入れ確認
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T16:47:25
---

## Task

## 目的
セットアップ専用に改稿された GUI (gui/server.py 等) を API レベルで検証し、gui/VERIFY.md を**全面的に書き直す**。
コードの修正はしない。レポートは段階書き込み (項目ごとに追記)。**ブラウザ操作ツールは絶対に使わない**。

## 手順
1. gui/VERIFY.md を新規内容で作り直し「セットアップGUI 検証」見出しと開始時刻を書く
2. MORNKANBAN_GUI_PORT=8803 python3 gui/server.py をバックグラウンド起動 (最後に必ず kill)
3. curl 検証 (各項目の PASS/FAIL とレスポンス断片をその都度追記):
   - GET /api/status → deps{herdr,claude,codex,python3} と install{cli,skill} と repo と herdr_env を含む
   - GET /api/board が 404 (旧エンドポイントが削除されている)。/api/policy と /api/card も 404
   - POST /api/install/cli → ok:true かつ ~/.local/bin/kanban がシンボリックリンクとして存在
   - POST /api/install/skill (force なし) → **409 が返ること** (この環境にはスキル導入済みのため。
     force での上書きは絶対にしない — 既存スキルは別システム生成物)
   - mkdir -p /tmp/kanban-gui-verify4 のうえ POST /api/projects → ok:true、
     POST /api/init → /tmp/kanban-gui-verify4/.kanban 生成、GET /api/projects に has_kanban:true で載る
4. サーバ kill → pgrep -f gui/server.py が 0 件であることを追記
5. 末尾に総合判定 (PASS/FAIL)

## 完了条件
- gui/VERIFY.md がセットアップGUI 検証の内容で全項目 PASS/FAIL と総合判定を持つ
- pgrep -f gui/server.py が 0 件

## History

### 2026-08-31 16:48:49 worker output (tail)

## POST /api/install/cli → PASS

`{"ok": true, "in_path": true}` (HTTP 200)。`~/.local/bin/kanban` がシンボリックリンクとして存在し、
このワークツリーの `kanban.sh` を指す。

```
lrwxr-xr-x ... /Users/matsufriends/.local/bin/kanban -> /Users/matsufriends/git/MornKanban/.kanban/wt/20260831-164725-9906/kanban.sh
```

## POST /api/install/skill (force なし) → PASS

既存スキルを上書きせず 409 を返す (force による上書きは実施していない)。

```json
{"ok": false, "error": "already installed (force で上書き)"}
```
HTTP 409

## プロジェクト登録 → init → 一覧反映 → PASS

`mkdir -p /tmp/kanban-gui-verify4` の後:

- POST /api/projects → `{"ok": true, "project": {"path": "/tmp/kanban-gui-verify4", "name": "kanban-gui-verify4", "has_kanban": false}}` (HTTP 200)
- POST /api/init → `{"ok": true}` (HTTP 200)、`/tmp/kanban-gui-verify4/.kanban` が生成される (KANBAN.md 等を含む)
- GET /api/projects → 一覧に `kanban-gui-verify4` が `has_kanban: true` で含まれる

```json
{"projects": [{"path": "/tmp/kanban-gui-verify", "name": "kanban-gui-verify", "has_kanban": true}, {"path": "/tmp/kanban-gui-verify2", "name": "kanban-gui-verify2", "has_kanban": true}, {"path": "/tmp/kanban-gui-verify4", "name": "kanban-gui-verify4", "has_kanban": true}]}
```
(既存の config に以前の検証プロジェクトが残っているが、今回の対象である verify4 は正しく反映)

## サーバ停止 → PASS

`kill 52843` 後、`pgrep -f gui/server.py` は 0件 (exit code 1)。

## 総合判定: PASS

全項目 PASS。セットアップ専用 API (`/api/status`, `/api/install/cli`, `/api/install/skill`, `/api/projects`, `/api/init`) は仕様通り動作し、
旧エンドポイント (`/api/board`, `/api/policy`, `/api/card`) は削除されて 404 を返すことを確認した。
コード修正は行っていない。

### 2026-08-31 16:49:42 review

score: 92 / threshold: 80

gui/VERIFY.mdの内容は現行gui/server.pyの実装(api_status/api_install_cli/api_install_skillの409分岐/route_apiの404メッセージ形式)と一致しており、~/.local/bin/kanbanのシンボリックリンクも実在確認済み。pgrep -f gui/server.pyは0件でサーバ停止も確認できた。コード側の差分もなし(git diff --statはgui/VERIFY.mdのみ)。減点理由: (1) 手順3冒頭で要求されている『各項目のPASS/FAILとレスポンス断片をその都度追記』のうち、追記が段階的(コミット単位)で行われた証跡がgit log上に1コミットしか見当たらず、当該レポートは現在も未コミットの作業ツリー差分のままで、途中経過が履歴として残っていない。(2) GET /api/projectsの結果に過去の検証残骸(verify, verify2)が混在しており、今回のverify4以外のプロジェクトが登録されたままの状態を注記のみで済ませている点はクリーンな検証環境とは言えない(タスク上必須ではないが再現性を下げる)。(3) POST /api/install/skillのforce=trueパスやapi_install_cliの再実行(symlink上書き分岐)など、コード上に存在する分岐の一部は未検証のまま(ただし指示範囲外なので致命的ではない)。総じて要求された5手順・完了条件はすべて満たしており、記載内容も実装と矛盾しない。
