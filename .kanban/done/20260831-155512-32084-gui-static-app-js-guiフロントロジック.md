---
id: 20260831-155512-32084
title: gui/static/app.js: GUIフロントロジック
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T15:55:12
---

## Task

## 目的
MornKanban ローカル GUI のフロントロジック `gui/static/app.js` を新規作成する (素の JS、ライブラリ禁止)。
index.html / server.py は別カードが同時に作っているため参照できない。以下の契約だけを頼りに実装する。

## DOM 契約 (index.html に存在する id)
dep-badges / project-list (select) / project-path (input) / btn-add-project / btn-init /
btn-secretary / jobs-input / btn-dispatch / tab-board / tab-policy / view-board / view-policy /
col-todo col-doing col-review col-done col-failed (ul) / card-title / card-body / card-backend (select) /
card-model / btn-add-card / policy-editor (textarea) / btn-save-policy /
card-modal (hidden div) / modal-content (pre) / modal-close / toast (hidden div)

## API 契約 (同一オリジン)
- GET /api/status → {"deps":{"herdr":bool,"claude":bool,"codex":bool,"kanban":bool},"herdr_env":bool,"repo":str}
- GET /api/projects → {"projects":[{"path","name","has_kanban"}]}
- POST /api/projects {"path"} / POST /api/init {"path"} / GET /api/policy?path= → {"content"} /
  PUT /api/policy {"path","content"} / GET /api/board?path= → {"states":{todo:[{id,title,attempts,file}],...}} /
  GET /api/card?file= → {"content"} / POST /api/card {"path","title","body","backend","model","threshold":""} /
  POST /api/secretary {"path"} / POST /api/dispatch {"path","jobs"}
- 失敗レスポンスは {"ok":false,"error":str} または非2xx。エラーは toast に表示

## 挙動
- 起動時: /api/status → dep-badges に各依存を span (class ok/ng) で表示。herdr_env が false なら
  「Herdr ペイン内で起動してください (秘書/ディスパッチャ操作不可)」を toast 表示し btn-secretary/btn-dispatch を disabled
- /api/projects → project-list を構築 (has_kanban でない項目は「(未init)」表示)。選択変更で board と policy を再読込
- btn-add-project: project-path の値を POST /api/projects → 一覧更新して選択
- btn-init: 選択中プロジェクトへ POST /api/init → board 再読込
- board: 選択中プロジェクトを 3 秒ごとにポーリングして5列を再描画。カード li は「title (attempts)」、クリックで
  GET /api/card → modal-content に表示し card-modal を表示。modal-close とオーバーレイクリックで閉じる
- btn-add-card: title 必須。POST /api/card (threshold は空文字) → 入力欄クリア、board 再読込
- btn-secretary / btn-dispatch: 確認なしで POST。成功時 toast に pane 名を表示
- tab-board / tab-policy で view の hidden を切替。policy 表示時に GET /api/policy、btn-save-policy で PUT
- toast(msg): 3秒表示して自動で隠す共通関数を作る

## 完了条件・検証
- node --check gui/static/app.js が通ること
- fetch 以外のグローバル依存なし。DOMContentLoaded 後に初期化

## History

### 2026-08-31 15:59:16 worker output (tail)



⏺ Write(.kanban-answer.md)
  ⎿  Wrote 13 lines to .kanban-answer.md
      1 gui/static/app.js を新規作成し
      2
        、fetch 以外のグローバル依存な
      4 - DOMContentLoaded で init() →
        ド・boardタブ表示 → /api/status
         → /api/projects → board 3秒ポ
      5 - /api/status: dep-badges に sp
        =false なら toast 表示し btn-se
        d
        を構築 (has_kanban=false は「
     … +26 lines

⏺ gui/static/app.js を新規作成した。node
  --check 通過。実装内容は index.html/API
  契約に完全準拠。

✻ Worked for 39s · done 3:59 PM
                          ◐ medium · /effort
──────────────────────────────────────────────
❯ 
──────────────────────────────────────────────
  20260831-155512-32084  kanban/20260831-15…
  ⏵⏵ accept edits on (shift+tab to cycle) ·
gui/static/app.js を新規作成した。

- 素の JS (IIFE, 'use strict')、fetch 以外のグローバル依存なし
- DOMContentLoaded で init() → 要素キャッシュ・イベントバインド・boardタブ表示 → /api/status → /api/projects → board 3秒ポーリング開始
- /api/status: dep-badges に span.ok/span.ng を描画。herdr_env=false なら toast 表示し btn-secretary/btn-dispatch を disabled
- /api/projects: project-list を構築 (has_kanban=false は「(未init)」表示)。change イベントで board/policy 再読込
- btn-add-project / btn-init / btn-add-card / btn-secretary / btn-dispatch / btn-save-policy を契約通りに実装
- board: li は「title (attempts)」、クリックで /api/card → modal-content 表示 → card-modal 表示。modal-close・オーバーレイクリックで閉じる
- tab-board / tab-policy で view の hidden 切替。policy 表示時に /api/policy 取得
- toast(msg): 3秒表示して自動非表示
- エラー時は {"ok":false,"error"} または非2xxのレスポンスボディの error を toast に表示

検証: `node --check gui/static/app.js` → OK

### 2026-08-31 16:00:00 review

score: 88 / threshold: 80

DOM/API契約はほぼ完全準拠。node --checkも通過し、fetch以外のグローバル依存なし、DOMContentLoaded後にinit()する構成も要件通り。減点理由: (1) btn-add-project/btn-init/btn-add-card/btn-secretary/btn-dispatch/btn-save-policyのクリックハンドラがasync関数を await せず呼び出しているため、api()内でthrowされたエラーがunhandled promise rejectionになる(toast自体は出るが、ブラウザコンソールにエラーが残る)。(2) addCard後にel['card-backend'].value=''/el['card-model'].value=''で選択をリセットしているが、<select>に空値のoptionが存在しない場合は選択状態が不定になりうる(index.htmlの実装依存、契約からは検証不能)。(3) openCard内のawait api()も同様にcatchされておらず、カード読み込み失敗時に未処理rejectionが発生する。いずれも機能上のtoast表示自体は正しく動作するため致命的ではないが、堅牢性の観点で減点。
