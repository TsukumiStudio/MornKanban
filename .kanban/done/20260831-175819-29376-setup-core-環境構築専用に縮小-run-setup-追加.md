---
id: 20260831-175819-29376
title: setup_core: 環境構築専用に縮小 + run_setup() 追加
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T17:58:19
---

## Task

## 目的
gui/setup_core.py を環境構築専用に縮小し、gui/setup_gui.py (tkinter) を1画面・実行ボタン1つに簡素化する。

## setup_core.py の変更
- 削除: load_projects / add_project / init_project と ~/.config/mornkanban/gui.json 関連コード
- 維持 (挙動不変): check_deps / cli_installed / skill_installed / in_worktree / install_cli / install_skill(force=False)
- 追加: run_setup() -> list[str]: in_worktree() なら ["refused: kanban worktree 内"] を返すだけ。
  そうでなければ install_cli() → install_skill(force=True) を順に実行し、各結果メッセージのリストを返す (冪等)

## setup_gui.py (tkinter) の変更
- 1ウィンドウのみ: 依存表示 (herdr/claude/codex ✓/✗)、CLI 状態、スキル状態、
  [セットアップ実行] ボタン (run_setup() を呼び結果を下部ラベルに複数行表示 → 状態を再描画)、[閉じる]
- プロジェクト関連 UI は全削除。追加のダイアログ・ポップアップを出さない (messagebox はエラー時のみ可)
- 下部に案内文「導入後はプロジェクトのペインで claude に『kanban の秘書として待機して』」

## 完了条件・検証
- python3 -m py_compile gui/setup_core.py gui/setup_gui.py
- python3 -c "import sys; sys.path.insert(0,'gui'); import setup_core; print(setup_core.run_setup())" が
  worktree 内では refused を返す (実インストールは走らない)
- setup_core に project/json の残骸 (load_projects 等) が無い (grep で確認)

## History

### 2026-08-31 18:00:57 worker output (tail)

  - 1ウィンドウ・依存表示・CLI/スキル状態・[セットアップ実行]・[閉じる] のみに縮小
  - プロジェクト関連 UI 全削除、案内文を追加

  検証

  - py_compile OK
  - run_setup() は worktree 内で refused のみ返却 (実インストール未実行) 確認済み
  - grep でプロジェクト/json 残骸なし確認済み

  コミット未実施。

✻ Cogitated for 43s · done 6:00 PM

──────────────────────────────────────────────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────────────────────────────────────────────
  20260831-175819-29376  kanban/20260831-175819-29376  Sonnet 5                                      /rc
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents
## 実施内容

### gui/setup_core.py
- 削除: `load_projects` / `add_project` / `init_project` / `load_config` / `save_config` / `project_entry`、`~/.config/mornkanban/gui.json` (`CONFIG`) 関連コード、`json`/`subprocess` の import
- 維持 (挙動不変): `check_deps` / `cli_installed` / `skill_installed` / `in_worktree` / `install_cli` / `install_skill(force=False)`
- 追加: `run_setup() -> list[str]`
  - `in_worktree()` なら `["refused: kanban worktree 内"]` のみ返す
  - それ以外は `install_cli()` → `install_skill(force=True)` を順に実行し、各結果メッセージをリストで返す (冪等)

### gui/setup_gui.py
- 1ウィンドウ構成に簡素化: 依存表示 (herdr/claude/codex ✓/✗)、CLI 状態、スキル状態、[セットアップ実行] ボタン (`run_setup()` を呼び結果を下部ラベルに複数行表示 → 状態再描画)、[閉じる]
- プロジェクト関連 UI (パス入力・追加ボタン・一覧 Listbox・kanban init ボタン) を全削除
- 追加のダイアログ・ポップアップなし (messagebox はエラー時のみ)
- 下部案内文を追加: 「導入後はプロジェクトのペインで claude に『kanban の秘書として待機して』」

## 検証結果
- `python3 -m py_compile gui/setup_core.py gui/setup_gui.py` → OK
- `python3 -c "import sys; sys.path.insert(0,'gui'); import setup_core; print(setup_core.run_setup())"` → `['refused: kanban worktree 内']` (worktree 内のため実インストールは走らず)
- `grep -n "load_projects\|add_project\|init_project\|gui.json\|CONFIG" gui/setup_core.py gui/setup_gui.py` → 該当なし (残骸なし)

## コミット
未実施 (要指示があれば細かい単位でコミットする)

### 2026-08-31 18:01:25 review

score: 85 / threshold: 80

実装・検証は仕様通り。ただし gui/setup_core.py の TIMEOUT = 30 (旧 init_project の subprocess.run 用) が未使用のまま残存しており削除漏れ。ワーカーの検証コマンド(grep対象語)にTIMEOUTが含まれておらず見逃された。機能上の実害はないが「残骸なし」の完了条件が厳密には未達。setup_gui.py側の簡素化・run_setup()の仕様適合・py_compile/run_setup()の実行結果は確認済みで正しい。コミット未実施。
