---
id: 20260831-191114-22625
title: VERSIONとCLI更新管理を実装
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T19:11:14
---

## Task

Objective: Add versioned distribution management to MornKanban and expose install/update/uninstall/version comparison from the CLI.

Target repository: /Users/matsufriends/git/MornKanban (work only inside the card worktree).

Requirements:
- Add a committed semantic VERSION source of truth (initial distribution version may be 0.1.0).
- `kanban --version` must print only the installed local version without network access.
- `kanban version` must print current version, latest published version, and comparison state.
- Treat raw VERSION on GitHub main as the latest published version because this repository currently has no tags or GitHub Releases. Allow a local/file URL override so tests do not require the network.
- Support `kanban install`, `kanban update`, and `kanban uninstall`; also support the same explicit commands through `kanban-setup.sh` for a first install before the symlink exists.
- Install must create/repair ~/.local/bin/kanban and install the kanban-dispatch skill for both Claude Code and Codex.
- Render the distribution version into installed skill metadata as well as the repository path.
- Update must compare versions, refuse dirty/detached/non-main checkouts, run `git pull --ff-only origin main`, then reload the updated installer and reinstall CLI/skills. Do not discard or stash user changes automatically.
- Uninstall must remove only installer-managed CLI/skills and retain the repository checkout and all project boards.
- Preserve bash 3.2 compatibility and use only the Python standard library.
- Resolve the real MornKanban path when `kanban.sh` is invoked through its installed symlink.
- Ensure kanban-setup.sh forwards command arguments to the Python CLI; this is a known silent-failure risk.
- Update README.md and gui/VERIFY.md for the new commands and version policy.

Verification:
- Extend tests/test_kanban_secretary.py (or add focused tests) for semantic comparison, symlink entry points, install/uninstall, a real temporary git fast-forward update, dirty-checkout refusal, argument forwarding, and end-to-end update reinstalling versioned skills under a temporary HOME.
- Run: bash -n kanban.sh kanban-setup.sh kanban-secretary.sh herdr-agent-worker.sh herdr-notify-secretary.sh
- Run: python3 -m py_compile gui/setup_core.py gui/setup_cli.py tests/test_kanban_secretary.py
- Run: python3 -m unittest -v tests/test_kanban_secretary.py
- Run the Codex skill quick validator if available; otherwise ensure SKILL.md frontmatter remains valid.
- Deliberately break argument forwarding and dirty-checkout detection in a copied workspace and confirm the relevant tests fail, then restore/pass.

Important: A previous dialogue-agent draft was stashed and is not part of HEAD. Implement independently from the current clean branch; do not inspect or apply that stash.

## History

### 2026-08-31 19:18:31 worker output (tail)

──────────────────────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────────────────────
  20260831-191114-22625  kanban/20260831-191114-22625  Sonnet 5              /rc
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents
# 結果

MornKanban にバージョン管理付き配布機能 (install/update/uninstall/version) を追加した。

## 変更ファイル

- `VERSION` (新規): 配布バージョンの正本、`0.1.0`
- `gui/setup_core.py`: `local_version`/`fetch_latest_version`/`parse_version`/`compare_versions`/
  `version_report`/`git_current_branch`/`git_is_clean`/`git_pull_ff_only`/`run_update` を追加。
  `_render_skill` が `__MORNKANBAN_VERSION__` も埋め込むようにした。`HERE` を `realpath` ベースに変更。
- `gui/setup_cli.py`: `install`/`uninstall`/`update`/`version` サブコマンドを追加 (引数なしなら従来の対話ウィザード)。
- `kanban-setup.sh`: `"$@"` を `setup_cli.py` へ転送するよう修正 (転送漏れは既知の危険としてタスク指定通り修正)。
- `kanban.sh`: シンボリックリンクを辿って実体ディレクトリを解決する `resolve_self_dir` を追加し、
  `--version`(ネットワーク無し・ローカル `VERSION` のみ)、`version`/`install`/`update`/`uninstall`
  サブコマンドを追加。
- `skills/kanban-dispatch/SKILL.md`: `__MORNKANBAN_VERSION__` プレースホルダを追加。
- `README.md` / `gui/VERIFY.md`: 新コマンドとバージョンポリシーを追記。
- `tests/test_kanban_secretary.py`: セマンティック比較、シンボリックリンク経由のエントリポイント解決、
  install/uninstall、実 git リモートに対する fast-forward update、dirty checkout 拒否、
  引数転送、バージョン付きスキルの再導入までの end-to-end を追加 (計19テスト、全て green)。
  既存の `SkillInstallerTests` は worktree 内実行のガードで落ちていたため `in_worktree` をモックして修正。

## 検証

- `bash -n kanban.sh kanban-setup.sh kanban-secretary.sh herdr-agent-worker.sh herdr-notify-secretary.sh` OK
- `python3 -m py_compile gui/setup_core.py gui/setup_cli.py tests/test_kanban_secretary.py` OK
- `python3 -m unittest -v tests/test_kanban_secretary.py` → 19 tests OK
- `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/kanban-dispatch` → valid
- 意図的破壊テスト: `kanban-setup.sh` の `"$@"` 転送削除、`run_update` の dirty チェック削除の両方で
  対応する新規テストが red になることを確認済み。その後 diff を復元し green に戻した。

## 未着手・注意点

- `install`/`update`/`uninstall` は `.kanban/wt/<id>` ワークツリー内からの実行を既存の `in_worktree()` ガードで拒否する仕様のまま (既存動作を踏襲)。このセッション自体がワークツリー内のため、テストは実チェックアウトを模した一時ディレクトリを作って検証している。
- `KANBAN_VERSION_URL` は `README.md` / `gui/VERIFY.md` に記載済みだが、実際のリモート GitHub raw URL への到達性はネットワーク制限のあるこの環境では未確認 (ロジックはテストで `file://` URL により検証済み)。

### 2026-08-31 19:20:23 review

score: 96 / threshold: 80

Comprehensive and correct implementation. Verified independently: all 19 unittest cases pass; bash -n and py_compile clean; VERSION=0.1.0 committed; DEFAULT_VERSION_URL correctly points at github.com/TsukumiStudio/MornKanban (matches actual origin remote); KANBAN_VERSION_URL file:// override works for network-free tests; compare_versions does semantic (not lexical) comparison; kanban.sh resolve_self_dir correctly follows symlinks (bash 3.2 safe, no arrays/associative); kanban-setup.sh now forwards "$@" -- reproduced the original silent-failure bug by removing it and confirmed ArgumentForwardingTests fails (2/2), then restored and confirmed green again; install/update/uninstall reproduced end-to-end via real temp git remote+clone (GitUpdateTests) and real HOME-scoped install/uninstall (InstallUninstallTests); dirty/detached-HEAD refusal in run_update verified never discards local edits; SKILL.md now embeds __MORNKANBAN_VERSION__ and quick_validate.py confirms the skill is still valid; README.md and gui/VERIFY.md updated consistently with the new commands and version policy. Also confirmed via `git stash` (restored afterward) that the SkillInstallerTests mock-of-in_worktree change was a legitimate fix for a genuinely pre-existing failing test at HEAD, not a weakening of coverage. Minor gaps: KANBAN_VERSION_URL is documented but its default GitHub raw URL was not reachability-tested against the live network (acceptable, since task explicitly asks for local/file URL override for tests); the worker's self-report is accurate and not overstated.
