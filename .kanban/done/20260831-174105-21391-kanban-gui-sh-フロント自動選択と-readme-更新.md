---
id: 20260831-174105-21391
title: kanban-gui.sh: フロント自動選択と README 更新
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 1
created: 2026-08-31T17:41:05
---

## Task

## 目的
kanban-gui.sh を「利用可能なフロントを自動選択して起動する」ものに書き換え、README の GUI 節を更新する。
フロント: gui/setup_gui.py (tkinter) / gui/setup_osa.py (macOS osascript) / gui/setup_cli.py (対話CLI) —
いずれも別カードが同時作成中。各フロントは「自分が動けない環境なら exit 2」という規約。

## kanban-gui.sh の仕様 (bash 3.2, set -euo pipefail)
1. python3 が無ければエラー終了
2. python3 -c "import tkinter" 2>/dev/null が通れば exec python3 "$REPO/gui/setup_gui.py"
3. 通らず、uname が Darwin かつ command -v osascript があれば exec python3 "$REPO/gui/setup_osa.py"
4. どちらも駄目なら exec python3 "$REPO/gui/setup_cli.py"

## README「## GUI」節の書き換え (他の節は触らない、英語 5〜7 行)
- ./kanban-gui.sh opens the best available setup frontend: a small tkinter window,
  native macOS dialogs (osascript) when tkinter is missing, or an interactive CLI wizard anywhere else
- Zero extra installs — python3 plus whatever the OS already ships
- Buttons/menu: install the kanban CLI, install the Claude Code skill, register projects + kanban init
- Day-to-day operation stays with the secretary (Secretary Bootstrap)

## 完了条件・検証
- bash -n kanban-gui.sh、test -x kanban-gui.sh
- grep -q osascript kanban-gui.sh && grep -q setup_cli kanban-gui.sh
- grep -q osascript README.md

## History

### 2026-08-31 17:41:54 worker output (tail)

       7 3. どちらも駄目なら `gui/setup_cli.py`
       8
       9 ### README.md「## GUI」節
      10 英語で以下の内容に更新 (他の節は未変更):
     … +20 lines

⏺ kanban-gui.sh を自動フロント選択方式に書き換え、README「##
  GUI」節を更新した。検証4項目すべて通過。gui/setup_osa.py / gui/setup_cli.py
  は別カードで作成中のため未存在(スコープ外)。

✻ Cooked for 41s · done 5:41 PM

──────────────────────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────────────────────
  20260831-174105-21391  kanban/20260831-174105-21391  Sonnet 5              /rc
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents
## 変更内容

### kanban-gui.sh
python3 の存在確認後、以下の優先順で利用可能なフロントを自動選択して起動するよう書き換えた。
1. `python3 -c "import tkinter"` が通れば `gui/setup_gui.py`
2. 通らず、Darwin かつ osascript があれば `gui/setup_osa.py`
3. どちらも駄目なら `gui/setup_cli.py`

### README.md「## GUI」節
英語で以下の内容に更新 (他の節は未変更):
- `./kanban-gui.sh` は利用可能な最良のフロントを開く (tkinter ウィンドウ / macOS ネイティブダイアログ(osascript) / どこでも動く対話CLIウィザード)
- 追加インストール不要 (python3 と OS 標準ツールのみ)
- ボタン/メニュー: kanban CLI インストール、Claude Code skill インストール、プロジェクト登録+kanban init
- 日常運用は引き続き秘書 (Secretary Bootstrap) が担当

## 検証結果
- `bash -n kanban-gui.sh` → OK
- `test -x kanban-gui.sh` → OK
- `grep -q osascript kanban-gui.sh && grep -q setup_cli kanban-gui.sh` → OK
- `grep -q osascript README.md` → OK

## 備考
`gui/setup_osa.py`, `gui/setup_cli.py` は別カードで作成中のため、本ワークツリー内には未存在。kanban-gui.sh は仕様通り exec するのみで、両ファイルの実体作成はスコープ外。

### 2026-08-31 17:42:30 review

score: 92 / threshold: 80

実装は仕様通り。bash -n / 実行権限 / grep 4件すべて通過を確認済み。kanban-gui.sh のフォールバックロジック (tkinter→osascript→CLI) は仕様と一致。README GUI節も英語4行で要求内容(自動選択の説明、追加インストール不要、ボタン/メニュー内容、秘書運用は別)を満たし他節は未変更。gui/setup_osa.py, gui/setup_cli.py が本ワークツリーに未存在なのはタスク定義通りスコープ外（別カードで作成中）。減点理由: README節が4行で仕様の『5〜7行』範囲を下回っている（軽微）。
