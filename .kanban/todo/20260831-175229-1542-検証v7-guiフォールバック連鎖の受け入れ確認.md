---
id: 20260831-175229-1542
title: 検証v7: GUIフォールバック連鎖の受け入れ確認
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 0
created: 2026-08-31T17:52:29
---

## Task

## 目的
GUI フォールバック構成 (setup_core / setup_gui / setup_osa / setup_cli / kanban-gui.sh) をヘッドレスで検証し、
gui/VERIFY.md を現構成の内容で全面的に書き直す。コードの修正はしない。段階書き込み。ウィンドウ/ダイアログは開かない。

## 手順 (各項目 PASS/FAIL と根拠を都度追記)
1. python3 -m py_compile gui/setup_core.py gui/setup_gui.py gui/setup_osa.py gui/setup_cli.py
2. import 副作用なし: python3 -c "import sys; sys.path.insert(0,'gui'); import setup_core, setup_cli" が何も起こさず通る
3. core ロジック: check_deps()/cli_installed()/skill_installed() の型、in_worktree()==True、
   install_cli() と install_skill() が worktree ガードで拒否、
   add_project→init_project を /tmp/kanban-gui-verify7 で実施し .kanban 生成
4. CLI フロント: echo q | python3 gui/setup_cli.py が exit 0、python3 gui/setup_cli.py </dev/null が exit 0
5. 選択ロジック: bash -n kanban-gui.sh、grep で tkinter / osascript / setup_cli の3分岐が存在
6. README: grep -q osascript README.md
7. 総合判定

## 完了条件
- gui/VERIFY.md が現構成の全項目 + 総合判定を持つ
- ~/.local/bin/kanban のリンク先が不変

## History
