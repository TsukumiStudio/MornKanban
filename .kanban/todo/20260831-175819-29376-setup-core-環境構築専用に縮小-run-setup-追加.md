---
id: 20260831-175819-29376
title: setup_core: 環境構築専用に縮小 + run_setup() 追加
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 0
created: 2026-08-31T17:58:19
---

## Task

## 目的
gui/setup_core.py を環境構築専用に縮小し、gui/setup_gui.py (tkinter) を1画面・実行ボタン1つに簡素化する。

## setup_core.py の変更
- 削除: load_projects / add_project / init_project と ~/.config/mornkanban/gui.json 関連コード
- 維持 (挙動不変): check_deps / cli_installed / skill_installed / in_worktree / install_cli / install_skill(force=False)
- 追加: run_setup() -> list[str]: in_worktree() なら ["refused: kanban worktree 内"] を返すだけ。
  そうでなければ install_cli() → install_skill(force=True) を順に実行し、各結果メッセージのリストを返す (冪等)

## setup_gui.py (tkinter) の変更
- 1ウィンドウのみ: 依存表示 (herdr/claude/codex ✓/✗)、CLI 状態、スキル状態、
  [セットアップ実行] ボタン (run_setup() を呼び結果を下部ラベルに複数行表示 → 状態を再描画)、[閉じる]
- プロジェクト関連 UI は全削除。追加のダイアログ・ポップアップを出さない (messagebox はエラー時のみ可)
- 下部に案内文「導入後はプロジェクトのペインで claude に『kanban の秘書として待機して』」

## 完了条件・検証
- python3 -m py_compile gui/setup_core.py gui/setup_gui.py
- python3 -c "import sys; sys.path.insert(0,'gui'); import setup_core; print(setup_core.run_setup())" が
  worktree 内では refused を返す (実インストールは走らない)
- setup_core に project/json の残骸 (load_projects 等) が無い (grep で確認)

## History
