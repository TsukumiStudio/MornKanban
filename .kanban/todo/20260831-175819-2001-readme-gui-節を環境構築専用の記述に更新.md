---
id: 20260831-175819-2001
title: README: GUI 節を環境構築専用の記述に更新
backend: claude
model: sonnet
threshold: 80
max_attempts: 3
attempts: 0
created: 2026-08-31T17:58:19
---

## Task

## 目的
README.md の「## GUI」節を「環境構築専用・1画面」の記述に更新する。他の節は触らない。

## 新しい内容 (英語 4〜6 行)
- ./kanban-gui.sh opens ONE setup screen (tkinter window, native macOS dialog when tkinter is missing,
  or a CLI prompt anywhere else) — environment setup only
- One "run setup" action installs the kanban CLI symlink and the Claude Code skill (idempotent)
- Project onboarding is NOT in the GUI: open a Herdr pane in the project and tell claude
  「kanban の秘書として待機して」 — the secretary runs kanban init and everything else
- Requirements: bash + python3 (the same as kanban.sh itself)

## 完了条件
- GUI 節から register projects / kanban init ボタンの記述が消えている
- grep -q 'environment setup only' README.md が真、grep -q 'bash + python3' README.md が真

## History
